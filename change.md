## SKILL

* 你思考过程可以使用英语

* 你是一个干了10几年go的资深程序员

* 每一段新加入的代码最好有对应的UT覆盖

## 补丁

- [x] 等待阶段的中央「等待玩家加入」卡片置于座位框下层，重叠时不会遮挡「等待加入」座位或拦截入座点击；本人座位继续保持最高座位层级。

- [x] 现金与筹码按钮统一复用原现金的金色装饰边框素材，改为「图标 + 数字 + ＋」单行提示；补码弹窗自动跟随按钮宽度，金额单独一行、加减按钮并排，避免窄面板溢出。

- [x] 现金与桌上筹码入口共用同一按钮组件：统一手机 112px / 桌面 128px 宽、48px 高、圆角、金色细边框与背景；现金按钮显示「点击充值」，筹码按钮保留「点击补码」，两者均使用加号与展开箭头。

- [x] 强化右上角桌上筹码的补码入口：金色「＋」标记、常驻「点击补码」提示和展开箭头，手机点击区域至少 44px 高；未开局且筹码为 0 时加强边框提示。点击打开原补码面板，支持点击外部或 Esc 收起，并显示展开状态。

- [x] 修复网络波动导致结算后整桌停住：服务端为全下发牌（每街 2 秒兜底）与结算后推进（7 秒兜底）增加计时器，正常浏览器仍维持 900ms / 5 秒动画节奏；同阶段广播不延长等待，过期计时器和房间销毁后不再推进，沿用预约入座、观战、局数上限与整场结算流程。客户端增加连接进度圈、心跳与连接/状态恢复超时检测，重连期间锁定牌桌操作，恢复最新快照后解锁；持续恢复失败 30 秒返回大厅、清除旧房间会话并继续后台重连，丢弃旧连接消息，恢复连接后释放自动找回的旧座位，避免再次被拉回房间。验证：Go 全量测试、新增推进测试及竞态检测、连接管理/恢复消息回归测试与前端类型检查。

- [x] 修复空座因余额不足而禁用时仍显示「等待加入」：显示实际受限原因，底栏展示余额、房间买入额与充值入口；充值满足买入后自动恢复可入座。空座操作以当前玩家列表判断是否已入座，避免结算后遗留座位 ID 阻止入座或误发换座。验证：入座/预约回归测试与前端类型检查；覆盖非房主余额 199/买入 200、充值恢复、旧座位 ID、准备和锦标赛限制。

- [x] 聊天消息、加入牌局提示及牌局日志按浏览器本地时区显示 24 小时制时间：后端发送带日期与时区的 RFC3339 时间，前端转换为本地 HH:mm。验证：前端类型检查、时区回归测试（北京时间、跨午夜、夏令时）及 Go 全量测试。

- [x] 修复位置入池率可能显示 1100%：旧版本曾累计 `VPIPByPos` 却未累计 `HandsByPos`，后续新手数与旧入池次数混用；同一手牌的多次翻前下注也曾重复累计。新增位置统计版本与完整性校验，位置入池率仅纳入发牌时至少 5 人参与的牌局；2～4 人局继续累计总手数及总入池率。旧位置统计未记录人数，无法按新口径拆分，在后续战绩合并时从有效新样本重新累计，其他总战绩不清空；前端对不完整记录显示「—」，不伪造 100%。每手发牌时固定统计位置，防止庄家弃牌离场后位置变化导致分子分母错位；UTG 使用大盲后首位实际参与者，CO 使用按钮前末位实际参与者，跳过未参与发牌的座位，其余非盲注中间位置合并为 MP。验证：Go 全量测试、前端统计回归测试和类型检查；覆盖 5～8 人位置映射与按钮跨座位边界、未准备座位跳过、2～8 人统计门槛、未参与发牌的座位不计入人数、弃牌不改变本手资格、UTG 再加注只计一次、庄家离场、1100% 旧数据、旧版本人数口径迁移及混合人数会话合并。

- [x] PC 大厅空列表卡片取消 560px 限宽，与上方房间数量/刷新栏等宽对齐；普通房间卡片仍保持紧凑宽度。

- [x] 最后一位真人主动退出或观战者离开后，空房立即从大厅移除并停止后台任务，机器人不会延长房间寿命；取消额外的空房回收计时。断线但未退出的座位仍保留 60 秒重连，超时退回剩余筹码后若无人则立即销毁；未完成的结算继续按原机制重试。回归测试覆盖最后玩家/观战者退出、机器人房、已离场但手牌未清空、排队加入、断线座位到期及退款。

- [x] 大厅房间列表改为独立滚动区域：页面固定为视口高度，工具栏、大厅标题、房间数量/刷新和页脚保持固定；房间卡片最大宽度收至 560px，列表支持鼠标滚轮、触摸与键盘滚动，空列表也在同一区域显示。浏览器以 18 个模拟房间检查桌面、320/390px 手机及横屏：滚轮移动列表而页面不动，最后一个密码房可访问并发送加入消息；类型检查通过。

- [x] 大厅与房间卡片按参考图统一为深海蓝风格：工具栏双层边框、黑桃标题和卡片徽记、青蓝加入按钮、金色盲注/买入金额，复用现有扑克背景与卡牌素材。保留房间状态、密码加入、观战人数、建房与刷新入口；手机采用紧凑字号，长房名截断且密码输入不会撑宽卡片。验证：类型/格式检查；浏览器模拟 320/390px 手机和桌面、中英文、普通/密码房加入消息、刷新、设置/建房弹窗及空列表。

- [x] 设置面板按参考图重做：深海蓝面板、青蓝描边与选中高亮、黑桃装饰、金边黑桃音量滑块，与大厅和牌桌风格统一。保留语言、音效、背景音乐、麦克风/他人音量、麦克风增强和 AI 降噪及原有保存逻辑；手机采用 12px 设置文字与标签/滑杆并排的紧凑布局，缩小行距和装饰区域；矮屏收起底部装饰文案，保留所有设置项，极小视口仍可滚动。补齐键盘焦点限制、Esc 关闭和关闭后焦点恢复。验证：前端类型检查；浏览器检查中英文、桌面、390/320px 手机与横屏布局，以及音量保存、静音、开关和弹窗交互；手机紧凑布局复查 390×664、375×667、320×568 中英文均无需滚动、无横向溢出。

- [x] 修复「开启 AI 降噪后对方完全听不到声音」。根因是挂载时机错误：原实现把 `KrispNoiseFilter` 挂到**已发布**的轨道上，而插件真正开始处理音频靠的是 SDK 在 `publishTrack` 时调用的 `onPublish()`；运行中挂载这条路走不到，而且它的 wasm worker 是异步初始化的、未就绪时 `setEnabled()` 会**抛错**（我们没传 `enableOnceReady`，所以也不会自动补启用）。结果是发送端挂着一条「处理器从未启动」的轨：本地看起来一切正常，对端收到的是死音。现改为插件文档支持的顺序——**先挂 processor 再 `publishTrack`**（`LocalTrack.mediaStreamTrack` 在发布时会取 `processedTrack ?? mediaStreamTrack`，顺序对了才带得出去）；切换开关时按新链路**重新发布**一条轨（`unpublishMic` 里显式 `stopProcessor()`：`unpublishTrack(track, false)` 只调 `stopMonitor()`，不会销毁处理器，不处理会泄漏 wasm worker）；启用改为**有界重试**（3s 内每 150ms 重试 `setEnabled(true)`，专治冷启动首次下载 wasm 的竞态），彻底失败则回退到未滤波的普通轨并把开关与提示一起回滚——**任何情况下都不会让玩家没声音**。顺带修 `reloadSettings()`：远端设置取回后若降噪偏好变了，已发布的轨也会按同一路径重建（原来只更 `state`，轨上挂的还是旧链路）。UT：`web/tests/voiceAudio.test.cjs` 26 项（新增 3 项：切换开关触发重新发布且旧轨处理器被销毁、wasm 就绪前持续重试直到成功、永久失败时回退普通轨 + 硬件降噪恢复开启 + 报 `noiseCancellationFailed`）。

- [x] 修正「回声消除」开关的命名。玩家的体感反馈是「这更像麦克风增强」——这是准确的：该开关下发的 `echoCancellation` 约束在 Chromium 里由**同一套 WebRTC 音频处理链**实现，戴耳机时本就无回声可消，能听见的变化其实是链路里其余几级（降噪成形 + 自动增益），体感自然偏向「增益」。中文标签定为「麦克风增强」（英文保留 `Echo & mic processing`：外放场景下它确实在消回声，不能让英文用户以为这个开关与回声无关），并删掉 `echoCancellationHint` 以及消费它的 `title`——原来那句「开启的是整套处理链、降噪与自动增益会一起变化」太长，漂浮提示在手机上也没有承载空间，而标签本身已经说清玩家能感知到的东西。仅改文案，设置结构与持久化字段不变。

- [x] 设置项同步到账号（跨设备）：玩家偏好不再只存在浏览器本地，而是同时镜像到账号（`gopoker:user:<uuid>` 的 `settings` 字段），换设备/清站点数据后登录即可恢复。后端把设置当作**不透明 JSON blob**（`server/settings.go`）：只校验「必须是 JSON 对象且 ≤4 KiB」后原样存取，布局归浏览器所有——新增一个设置项无需改服务端，也不必维护一份要同步的镜像结构体；`maxSettingsBytes` 防止恶意客户端灌爆存储。消息：`set-settings` → `settings-result`（失败走既有 `error` 通道），设置只在 `user-info` 的**本人**回复里下发（他人资料不带，避免泄漏「我屏蔽了谁」这类偏好）。同步项：语言、音效音量、BGM 音量、提示音设置、语音设置（麦克风/他人音量、回音消除、AI 降噪、按人屏蔽名单）。前端 `web/lib/settings.ts` 负责收集/应用/上报：本地即时生效、后台 1.5s 防抖合并上报（拖滑块只存一次），离线改动等 socket 恢复后补发；**登录前一律不上报**（否则服务端回 `not logged in`,会被共享错误 toast 弹成假报错）；登录时**以账号为准**，但账号从未同步过（新玩家/本功能之前的存量账号）则用本地设置播种，确保没人丢配置。各模块反向订阅重载（`voice.reloadSettings()`、sfx 音量缓存失效、sound 按需读取），模块间无循环依赖。UT：`backend/server/settings_test.go`（校验规则 11 例、只有本人能拿到、未同步账号不返回空对象）、`web/tests/settings.test.cjs`（12 项：收集/类型校验/畸形 blob 拒绝/防抖合并/离线补发/登录前不发/关闭的 socket 不抛错）；端到端实测：注册 → `set-settings` → 新连接登录 `get-user` 取回完全一致。

- [x] 语音的 TLS 接入（https 部署必需）：新增 `deploy/Caddyfile` + 可选服务 `livekit-proxy`（profile `voice-tls`，**三个 compose 都加了**——服务器实际用 `docker-compose-dev.yaml` 部署，Go 应用自己在 443 终止 TLS、无宿主级反代，所以反代必须随 dev 栈一起起），为 SFU 提供 wss 端点。**动因（均为实测，非推测）**：①浏览器会拦截 https 页面发起的 `ws://`（混合内容）；②`livekit-server` 自己不能终止 TLS——给配置加 `tls:` 段会被解析器直接拒绝（`field tls not found in type config.Config`,v1.13.7)。实现上让 Caddy 复用应用已有证书：`./certs` 以与应用**相同路径** `/app/certs` 挂入代理，于是 `TLS_CERT_FILE`/`TLS_KEY_FILE` 原样生效，不引入第二套证书流程；证书变量写成 `${LIVEKIT_TLS_CERT_FILE:-${TLS_CERT_FILE:-}}`（已实测 compose 支持嵌套默认值），autocert 部署可用 `LIVEKIT_TLS_*` 覆盖为 `/app/certs/<域名>/<域名>.crt|key`；站点写成 `:{$LIVEKIT_PROXY_PORT:7443}` 通配形式，容器内无需再写域名。本地已用自签证书实测：经 Caddy 的 `https://…/rtc/validate` 返回 `200 success`,WebSocket 升级路径 `/rtc` 返回 `401`（证明升级请求确实透传到了 LiveKit）。文档：`.env.example`/`.env` 写明启用方式与「TLS 只管信令、UDP 媒体段仍须开放」,`deploy/HARDENING.md` 补上 `LIVEKIT_PROXY_PORT` 放行。

- [x] 修复「两个浏览器开语音听不到」的两个环境级根因。①**LiveKit 端口不可达**：compose 里用 `network_mode: host` 在 Docker Desktop 上无效（日志显示 LiveKit 绑在 VM 网段 192.168.65.3），宿主机 7880 根本没监听，浏览器连不上。现改为显式发布 `7880/tcp`+`7881/tcp`+UDP 媒体段，并把配置生成逻辑抽到 `deploy/livekit-entrypoint.sh`：该脚本根据 `LIVEKIT_URL` 推导要对外通告的地址（`ws://localhost:7880`→`node_ip: 127.0.0.1`、`ws://192.168.x.x:7880`→该 IP、域名/wss→`use_external_ip: true` 走 STUN），否则容器会通告自己不可路由的 IP。踩坑记录：`node_ip` 一开始被追加到配置末尾，落进了 `keys:` 段（LiveKit 把它当成 API key 并报 secret 太短），已改为在 `rtc:` 块内按需输出。②**`.env` 挂载 inode 失效**：compose 以文件形式挂载 `.env`，绑定的是容器启动时的 inode；编辑器保存是「新建+rename」,旧 inode 被删除后容器读取会 `I/O error`——因此在 `.env` 里后来新增的 `LIVEKIT_*` 对已运行的后端完全不可见，后端没有凭据只能回 `ok:false`（前端提示「此服务器未开启语音」）。现把 `LIVEKIT_API_KEY/SECRET/URL/TOKEN_TTL_HOURS` 与 `REDIS_URL` 一样通过 compose `environment:` 传入（godotenv 不覆盖已存在的环境变量），并在 `.env.example` 顶部写明「改完 .env 需 `docker compose up -d --force-recreate`」。③清理：删除跨版本遗留的 `go-poker-coturn-hot` 与我早期冒烟测试残留的无名 livekit 容器，停掉 .env 挂载已损坏的旧 hot 后端。④新增 `scripts/voice-smoke.mjs`：无浏览器端到端自检——注册两个账号、进同一房间、各自取 token 并交给 LiveKit `/rtc/validate` 校验（实测两账号均通过，token 指向 `ws://localhost:7880`、房间 `gopoker-<房间名>`、TTL 6h）。

- [x] 麦克风 AI 降噪（LiveKit Krisp 插件）+ 修复断线后麦克风不再发布。①`@livekit/krisp-noise-filter` 接入：⚙️ 设置面板新增「AI 降噪」开关（默认关，本地记忆）；开启时 `LocalAudioTrack.setProcessor(KrispNoiseFilter())` 把模型挂在已发布轨道上，浏览器自带降噪同步关闭（两级串联会削坏语音），关闭或失败时回退到浏览器降噪——保证「任何时候都有且只有一个降噪在跑」。插件为 ~6 MB 内联 WASM，用 `import()` 懒加载：只有主动开启的玩家才下载（构建后是独立 5.9 MB chunk，首屏 244 kB 不变）。必须显式 `setEnabled(true)`：LiveKit 只在「发布时已挂 processor」的情况下通过 `onPublish` 自动启用，运行中开启会被静默跳过；`setEnabled` 失败则 `stopProcessor()` 摘掉半成品。处理器要求轨道带 AudioContext（livekit-client 在 Web 上直接抛错），所以发布时把麦克风处理链的 AudioContext 一并传入——同一 context 也保证采样率一致。②修复严重 bug：LiveKit 异常断线（网络抖动、服务重启、同账号双标签页被踢）后重连，`Disconnected` 处理器没有清空 `published` 引用，导致 `publishMic()` 被陈旧引用短路——界面显示「麦克风已开」但没人听得见，必须手动关麦再开才能恢复。现已在断线时清空。UT：`web/tests/voiceAudio.test.cjs` 23 项（新增 4 项：开关双向生效并同步约束、开麦前已开启时采集即不带浏览器降噪、不支持/挂载失败/启用失败三种回退、断线重连后重新发布；其中重连用例已验证在移除修复后会失败）；`npm run type-check` 通过。真机上 Krisp 是否可用仍需实测（插件会对轨道调用 `applyConstraints`，而我们的发布轨来自 WebAudio 图，若不支持会走回退路径）。

- [x] 语音依赖瘦身：后端不再引入 `livekit/protocol`（为签一个 JWT 拖进 protobuf/psrpc/otel 等约 300 个包，冷编译 9.7s 且 Docker/CI 上更糟），改用其同款底层库 `golang-jwt/jwt/v5` 按官方 token 契约手写签发（`iss`=API key、`sub`/`identity`=账号、`name`、`video` grant 仅允许麦克风音源发布/订阅并显式关闭数据通道、`iat/nbf/exp`）。删除依赖前用官方 SDK 与手写实现各签一个 token 逐字段比对通过（临时等价性测试，验证后移除）；模块图 683 行 → 60 行，依赖包 540 → 245，冷编译（`go build -a ./server`）9.7s → 4.1s。正式 UT 改用 `jwt/v5` 解析验证：签名/签发者/过期校验、授权范围（房间前缀、仅麦克风、`canPublishData` 显式 false）、TTL、未配置降级 `ok:false`、错 secret 拒绝。

- [x] 语音模块重构为 LiveKit（livekit/client-sdk-js + livekit-server）：删除浏览器间 P2P mesh 与 coturn STUN/TURN，所有语音媒体改经一个自托管 LiveKit SFU 转发。服务端删除 `voice.go`/`turn.go`（`voice-signal`/`get-ice-servers`/`ice-servers` 消息一并移除），新增 `livekit.go`：`get-livekit-token` 为已登录且在房间内的连接签发短期 JWT（identity=账号 UUID、room=`gopoker-<房间名>`、仅允许麦克风音源发布/订阅、禁数据通道，TTL 默认 6 小时），未配置 `LIVEKIT_API_KEY/SECRET` 时返回 `ok:false`，前端提示「此服务器未开启语音」。前端 `web/lib/voice.ts` 改用 `livekit-client` 的 `Room`：保留原有 WebAudio 麦克风链路（增益/限幅/回声消除/Audio Session 切换）并作为 `LocalAudioTrack` 发布，远端按 identity 挂 `<audio>` 元素，token 临期自动续签、断线 1 秒重连；`web/actions/actions.ts`、`providers/WebSocket.tsx` 同步换为 `livekit-token` 消息。部署：三个 compose 的 `coturn` 服务替换为 `livekit`（同样 voice profile + host 网络，entrypoint 生成配置并校验密钥对，UDP 50000-50100 + 7880/7881），`.env` 换 `LIVEKIT_*`（浏览器可达的 `LIVEKIT_URL` 等）。UT：后端 `livekit_test.go`（token 授权/身份/房间前缀/未配置降级/密钥校验），前端 `voiceAudio.test.cjs` 19 项全部保留通过（mesh 用例改为 SFU 连接/发布/远端播放）。

- [x] 回声消除默认关闭（保留已保存的选择）；开麦时通过 `applyConstraints` 更新原采集轨道，保留 WebAudio 处理链、输入增益与发送轨道，快速切换顺序执行，失败时恢复原设置。音效/麦克风补齐 Safari `interrupted` 恢复和后续用户手势重试；自动增益与降噪独立于回声消除保持请求开启。麦克风音量扩展为 0–300%，加入峰值压缩及中英文提示以支持偏小的原始输入。19 项语音回归测试覆盖默认值、参数切换、失败恢复、异步取消、音效恢复与增益保存；实际 iPhone 音量仍需真机验证。

- [x] 修复 iPhone 关闭麦克风后仍停留在通话音频模式：通过 Audio Session API 在开麦时使用 `play-and-record`，关麦但仍收听时恢复 `playback`，语音全关时恢复 `ambient`；游戏音乐和音效继续按原音量播放。关闭收听时暂停远端音频，离房时释放媒体元素；补齐授权/音频初始化/回声消除重启期间关闭语音的异步取消，以及处理节点初始化失败时的 AudioContext 清理。前端回归测试：`web/tests/voiceAudio.test.cjs`；实际 iPhone 输出路由仍需真机验证。

- [x] 限制局中主动亮牌：本人已 all-in，且本手中仍有筹码可操作的玩家最多只剩 1 人时才显示亮牌按钮，后端同步校验；包括四人局中三人 all-in，以及全部 all-in 的情况。保留正常多人摊牌与手牌结束后赢家秀牌，补充引擎及 WebSocket 消息回归测试。

- [x] 修复局中预约入座、手牌结束后老玩家无法取消准备：收尾时同步玩家状态与 `In` 标记，保留自动续局所需的准备状态。回归测试覆盖弃牌结束、正常摊牌，以及新玩家入座后老玩家取消准备、重新准备再开局：`backend/poker/action_boundary_test.go`、`backend/server/reserve_test.go`。

- [x] 修复自动结算后减少人数重新开局时的数组越界：`Game.Reset` 清理庄家、盲注、行动位置及下注轮状态，保留房间配置。消息处理层回归测试覆盖 6 人打满 3 手结算后，2 人重新入座准备、正确扣除盲注并继续下一手：`backend/server/settlement_reset_test.go`。

- [x] 修复第二轮审查的五项问题：弃牌只允许下注阶段仍在局内的行动者执行，重复请求不再重复分配底池；下注先校验余额与溢出，再计算最小加注和重开加注权；服务端及引擎禁止牌局中取消准备、换座，正常离场仍可执行；自动结算的退款或共享战绩保存失败时保留房间并自动重试，按座位跳过已成功退款，期间冻结游戏操作并延后空房回收；房间内禁止重新登录、注册或切换恢复账号，会话接管也检查房间边界。测试覆盖原始 WebSocket 请求、部分退款成功后的重试、历史保存失败、定时恢复和退出时取消重试：`backend/poker/action_boundary_test.go`、`backend/server/action_boundary_test.go`、`backend/server/settlement_retry_test.go`。

- [x] 修复审查发现的四项房间完整性问题：已离场座位不可重连或再次返款，所有离场/结算路径按座位 UUID 防止重复支付；房主只能在两手之间重置，先返还筹码（含待到账补码）并保存战绩，退款失败保留未退款座位供重试；普通/预约/机器人入座统一使用原子建座接口，抢座、换座、开局、补码和重置通过房间锁协调，已占座位与扣款失败不再生成无效座位；进入另一房间或建房前必须先退出当前房间，同房间重复加入不重复注册。回归测试：`backend/server/room_integrity_test.go`、`backend/poker/seating_test.go`，覆盖重复离场、退款重试、并发抢座、重置与补码并发及跨房间请求。

- [x] 接入带对手建模（OM）的 Deep CFR checkpoint：`table` 在每次成功 `Bet/Fold`（含机器人和行动超时）前抓取局面，按玩家记录本手的 5 类 `action_id` 与训练同构的 25 维上下文；AI 决策请求新增 `opponent_histories`，排除行动者自身并在新手牌/新会话时清空。推理服务改用 `create_agent_for_checkpoint` 自动加载普通或 OM agent，OM 推理在请求级隔离下通过 `record_opponent_action` 重放历史并注入对手特征，普通 checkpoint 与空历史保持兼容。UT：Go `TestAIHandActionHistory` / `TestAIDecideSendsOpponentHistories`；模型仓库 `test_inference_server.py` / `test_opponent_modeling_features.py`。

- [x] 修复 PC 大厅顶部状态条过宽：大厅主体的 880px 外框含左右各 32px 内边距，因此顶部账户/工具栏在桌面端按实际内容宽度 816px 布局，两侧边界与房间卡片精确对齐；手机端宽度规则保持不变。

- [x] 缩短创建房间界面：房间名与密码并排，盲注和买入压为三列；人数、局数、操作时限、锦标赛和 AI 等低频参数收进带当前摘要的「更多设置」，展开后继续使用紧凑网格，所有原有参数与校验保持不变。

- [x] 大厅改为房间优先的手机布局：移除占据首屏的居中 GoPoker 品牌区，将「大厅 / 找个房间，一起玩牌」和主操作「新建房间」直接放到顶部；账户与工具栏合并为一个表面，刷新收进房间标题并降为轻量按钮。空房间状态增加「创建第一个房间」入口；房间卡片集中展示人数上限、盲注、买入、局数、行动时限、观战人数及进行中/锦标赛/AI/密码状态。`table-list` 同步补充这些房间配置字段，并由 `TestTableInfoIncludesLobbyConfig` 覆盖。

- [x] 修复快捷加注金额重复时点击无反馈：最小、半池、满池、两倍池可能因最小加注或短筹码限制得到同一金额；选中态现记录玩家实际点击的选项，不再从最终金额反推。四个按钮即使金额全都相同，也会分别切换金色选中态；拖动滑杆或使用加减键后清除快捷选中态。

- [x] 强化玩家下注金额：下注胶囊改为近黑高对比表面、亮金描边与微光，放大面额色筹码和粗体金色数字；恢复对手缩小座位中的反向缩放，让下注额保持接近本人座位的可读尺寸，金额变化继续使用筹码弹出动画。

- [x] 6 人以上牌局禁止 AI 机器人：Deep CFR 模型只训练过 6 人局，`create-table` 的 `botType "ai"` 在座位数超过 6 时直接拒绝（`validateBotType`，返回「AI 机器人最多支持 6 人牌局」，且在房间创建之前校验）；`aiDecide` 内部同样以 `aiModelSeats` 常量守卫。前端建房对话框的人数输入超过 6 时，「AI 机器人」开关禁用并显示原因，已勾选的自动退回普通机器人，提交时再守卫一次（防绕过）。UT：`TestCreateTableAILimitedToSixSeats`（7/8 人拒绝、≤6 人通过、普通房不受座位限制、座位上限不遮蔽健康检查）。

- [x] 机器人分类型，建房时选择：不再「AI 服务可用就把所有机器人切到 AI」——每个房间创建时固定机器人类型（`create-table` 新增 `botType`，`"normal"` 默认=内置启发式 / `"ai"`=Deep CFR 推理服务），服务端 `table.botKind` 保存并在 `table-list`（房间条目 `botType`）与 `update-game` 中下发；`decideBotAction` 按房间类型决策，AI 房间遇到服务报错时单步回退启发式。选择 `"ai"` 的门槛：`AI_INFERENCE_URL` 已配置**且**其 `/healthz` 探活通过（`aiServiceAvailable`，结果缓存 10s，`table-list` 携带 `aiAvailable`）；建房时服务不健康返回「AI 机器人服务不可用」且不创建房间，未知类型返回「未知的机器人类型」。前端建房对话框新增「AI 机器人」开关（仅 `aiAvailable` 时可选，副文案说明服务状态），大厅房间条目对 AI 房间显示徽标。AI 房间的机器人命名 `AI Ace…`（账号 `bot-ai-ace`，与普通机器人命名空间分开、计分板各自合并），头像 🧠；普通房间保持 `Bot Ace` / 🤖 不变。顺带修复 `aiDecide` 的一个 bug：翻牌前引擎把公共牌预分配为 5 个 0 槽位、未发牌玩家手牌也是 0，原 `aiCardFrom` 遇 0 直接报错导致 AI 机器人翻牌前永远回退启发式，现在 0 值视为「未发牌」跳过。UT：`backend/server/ai_bot_test.go`（健康探活与缓存、建房的 botType 校验与服务不可用拒绝且不留房间、按房间类型决策：普通房绝不请求推理服务 / AI 房请求且服务挂掉时回退、AI 房命名头像与 update-game/table-info 携带 botType、普通房行为不变）。

- [x] 背景音乐默认音量下调为 5%（原 15%）：新用户进大厅 / 房间的 BGM 起播即 5%，可在 ⚙️ 设置里调大；设置面板中 BGM 的「静音恢复」与默认保持一致（恢复为 5%）。已有本地记忆的用户不受影响（沿用其保存值）。

- [x] 房间聊天改为「从按钮长成真正的对话界面」：点击底部聊天入口后，整个面板从按钮一体向上长出（不再是悬在按钮上方、带 8px 间隙的独立浮窗）；同时把底部那一排「say something…」触发器直接替换成真正的输入条（表情 / 输入 / 发送，输入条抽为 `ChatComposer`），消息区在上、输入条贴底，收起后还原为按钮行（未读角标保留）。与「战绩」面板同一种自按钮拉伸的交互。已拆分的 `Chat.tsx` 支持 `noComposer`，其它布局（桌面旧式 / compact）不变。
- [x] 修复手机端准备状态与筹码数量重叠：所有等待阶段座位（包括本人）都利用头像下方的空白把第二行向左扩展，筹码固定在左侧、准备操作或状态固定在右侧；开局后的座位布局不变。

- [x] 修复点击发送后聊天框自动收起：发送键因输入被清空而禁用时会触发 `relatedTarget = null` 的 blur，现不再把这种焦点变化当成离开面板；点击外部与 Esc 收起保持不变。

- [x] 聊天展开层移到整排功能按钮上方：聊天、战绩和「…」入口始终在底部同一行完整可见；聊天面板按房间底栏宽度向上展开，并以聊天按钮为动画原点，不再把聊天内容与入口包成一个覆盖左侧的大容器。

- [x] 修复聊天与战绩收起后仍留下整块空白：固定高度面板作为 `0fr` 网格子项时仍产生固有高度，现改为 `max-height: 0` + `overflow: hidden` 的折叠方式；收起后严格只保留底部入口按钮，展开时聊天最多 480px、战绩最多 300px。

- [x] 轻量化牌桌聊天：默认内容区高度降为 260px（含底部入口总高约 306px），顶部手柄可在 220–480px 内拖动或用方向键调整；战绩按内容增长并封顶 300px，超出后内部滚动；Tab 改为文字与金色短线并明确为「牌局记录」；系统事件居中弱化，玩家消息按本人/他人分侧显示头像气泡；输入、表情入口和发送键合为一个输入条，空内容禁用发送并关闭自动填充；增加常用表情与中英快捷短语。

- [x] 房间底栏聊天与战绩改为和观战列表一致的按钮向上展开交互；入口和底栏高度保持不变，支持点击外部、关闭键及 Esc 收起，大厅/历史战绩弹窗保持原样。

- [x] 重组 Profile「详细数据」：核心四项保留数据卡层级，牌局行为改为 2×2 网格，位置入池率改为 BTN/SB/BB/UTG/MP/CO 的 2×3 紧凑面板，移除逐字段横线列表。

- [x] 未开局时点击自己的整张玩家卡即可准备或取消准备，扩大手机触控区域；头像点击仍只打开 Profile，内嵌按钮阻止冒泡避免重复切换。

- [x] 将自己的「准备」操作内嵌到玩家卡右下角：未准备为轻量金色操作，准备后变为薄荷绿状态并弱化按钮感；玩家卡不再整体打开 Profile，只有头像可打开。

- [x] 统一房间底栏聊天、战绩和「…」按钮的深蓝灰配色、描边、阴影与交互反馈。

- [x] 手机竖屏牌桌进一步拉长并收小上下圆角，保持座位、动画及操作栏坐标不变；固定观战预约前后的状态文字和按钮区域高度，避免卡片轻微跳动。

- [x] 跟注按钮单独放大并高亮金额，保持过牌样式；加注面板「确认加注」简化为「确认」。

- [x] 未开局隐藏观战身份卡；入座后底栏展示战绩，观战列表收进「…」。列表从按钮向上展开，显示观众头像和昵称，长昵称省略、超长列表滚动。

- [x] 观战界面改为底部身份/预约状态与房间信息卡，支持预约与取消，展示实时观战列表；观战和已入座玩家共用底部聊天入口，聊天与牌局日志在底部面板切换；预订座位使用虚线卡片，牌桌与动画按底栏实际高度适配。

- [x] iPhone 房间背景补到 html/body 根画布，Safari 状态栏/地址栏声明同色 theme-color；保留原 viewport、安全区域和牌桌/按钮布局。浏览器原生栏的纹理显示仍由 Safari 控制。

- [x] 修复首次进房/充值素材迟到：按场景预加载并等待图片解码，复用请求、补全充值图片清单；充值 PNG 转为透明 WebP（约 5.06 MB → 185 KB），原稿保留在设计目录；加载等待最多 8 秒，单文件超时可重试，进度按资源完成计算。

- [x] 功能按钮固定在右下角横排，操作栏上移到功能按钮上方；竖屏座位向桌沿外侧展开，拉开上下方座位与斜角座位的间距，并同步筹码动画。

- [x] 手机操作栏改为单排紧凑按钮，辅助操作收为横排，省出的高度用于拉长牌桌；桌面「GOOD CARD & GOOD FRIEND」改为弧形排字。

- [x] 按 `web/assets-src/room_new_design.png` 调整牌桌：手机竖向长桌，按可用宽高缩放座位并预留工具栏与操作区；桌面使用深绿绒面、木质桌沿与内外层阴影；底池、公牌与筹码动画共用布局坐标，横屏和窗口缩放同步适配。

- [x] 按统一效果图更新大厅、新建房间与历史战绩：大厅账户栏、工具栏和空房间提示采用深蓝灰风格；新建房间使用独立字段标签、锦标赛开关与青蓝主按钮；历史记录分层展示玩家、房间、时间及净输赢，保留原有详情入口。兼容中英文及手机布局，牌桌保持原版。

- [x] 赢得底池的结算不够明显，并且谁赢了也不明显，我们可以适当加入加注或者跟注的动画来体现出玩家已经做出行为

- [x] 如果所有人退出了房间，该房间应该在1分钟之内销毁，然后用户重新登录，需要连接到之前的session，而不是当做一个新的观众加入。

- [x] 需要添加观众功能。

- [x] 把卡片 T 换成 10

- [x] 我们需要一个简单大厅功能，选择想要进入的房间，

- [x] 账号注册：每个人需要注册一个账号，只需要用户名就行（不能重复）

- [x] 然后同一个用户重连，如果之前在房间，需要进入该房间；如果没有在房间进入大厅然后可以自行选择一个房间，或者创建一个房间。

- [x] 每个用户可以在大厅充值筹码

- [x] 带入桌的筹码是提前用户输入，牌局当中可以选择补码，然后下一局会生效

- [x] 在用户退出牌局时，我们会记录本次的战绩，比如玩了多少轮，弃牌率、3bet率、最大赢得的底池、胜率、不同位置的入池率等等。

- [x] 我们需要把用户信息（用户名、筹码数量、对局信息等等）记录到存储中，可能会需要存储服务。

- [x] 房间密码、观众人数

- [x] 我们需要用户创建密码了，然后采用业界的密码验证形式，有API或库更好

- [x] 我们可以设置头像

- [x] 我们还需要一个可以查看用户的战绩的界面，显示我刚才说的那些信息。并且我们还可以查看别人的战绩，通过点击别人的头像

- [x] 在大厅界面添加好友，可以通过点击好友来查看其信息

- [x] 我们需要退出登录的按钮，以此达到一个ip可以登录不同的账号

- [x] 对于一个不在线的玩家，我们会有一个timeout时间，如果超过1分钟离线（sesion 断开），我们将会让直接弃牌，并且在下一局开始之前把他请出房间。

- [x] 我们应该支持语言选择，English/中文（有现成的API么？）

- [x] 我们可以上传不超过10MB的图片当做头像，我们会压缩为1024x1024, 512x512, 256x256, 128x128, 64x64进行存储，然后我们会选择一个合适的显示（取决于屏幕大小）

- [x] 除了总体的数据统计，我们还需要房间的数据统计（用户可以浏览房间每个人在当前房间下的数据）。

- [x] leave 和 reset 的颜色太不明显，reset按钮不应该存在了

- [x] buy in 和 充值支持1000的买入显然不太合适，充值应该支持 200,500,1000，5000

- [x] 创建一个房间的时候，我们需要选择盲注大小，以及buy in的次数。并且设置buy in的固定金额

- [x] 牌桌上需要显示头像和名字（用一个圆角矩形框住，然后筹码显示在头像的右边）

- [x] 在创建房间时，我们可以选择人数，并且根据人数改变座次（需要调整UI），最多支持8人

- [x] 不允许重复注册

- [x] 密码错误需要有toast提示

- [x] 我们需要一个历史战绩面板列出玩家出战情况，以列表显示包含房间号，玩家信息，并且点击玩家信息可以查看该玩家信息和战绩。

- [x] 默认的小盲设置为1，大盲设置为2，默认玩家数6，玩家筹码200，buy in 2次，每次buy in 200.

- [x] 玩家创建房间时可以输入参数

- [x] my stat 可以去掉，然后我们点击左右上角的头像就是个人战绩，然后再点击头像可以更换头像

- [x] 对局没有结束中途离开，会在该玩家回合直接弃牌并结算，并且把对局记录加入到该玩家的记录中

- [x] 历史战绩列表，虽然现在可以显示历史战绩，但是点击无效。我们希望点击历史战绩显示该对局玩家当前局的数据信息

- [x] 当房间已经开始游戏之后，新加入的玩家不能够上桌。我们需要让新玩家准备之后可以下场开始之前进入

- [x] 支持边池

- [x] all in 之后，有些玩家不能行为，也不能跳过他的回合

- [x] 充值键在手机上挡住手牌

- [x] 玩家在all in 之后可以选择秀牌

- [x] 我们可以规定打多少局 10/20/50/xxx, 局数结束之后出现结算画面

- [x] 结算画面包含每个人的输赢情况，然后公布最大底池的赢家和金额

- [x] 有人提前退出不会中断当前牌局，退出的人会自动弃牌并且变灰，然后当前牌局结束之后，大家进入未准备状态（牌局数并不清零）。

- [x] 增加提前结算按钮，然后投票表决是否提前结算，如果票数过半就提前结算

- [x] 调整UI，我们可以合并过牌和跟注两个按钮，把过牌按钮变成all in 按钮

- [x] 对于每次玩家切换，和发牌，我们都需要加入动画来使得没有那么突兀

- [x] 由于其他玩家都all in，当前玩家的操作框会常亮，这样起不到提醒作用，我们应该做的是，有一个动画轮询每个玩家（虽然每个玩家不需要任何操作），然后轮到该玩家之后，其操作框才亮起。

- [x] bug: 当一位玩家输光之后，全员应该进入未准备状态，而不是在弹出“赢家”卡住

- [x] 我们需要为游戏加入bgm和音效，然后为游戏增加音量调整（bgm和音效）。我们支持女生和男生两种音效（我觉得在网上会有资源或者现成的API）。

- [x] 音量调整需要加入到设置面板中，把语言选项也加入到设置按钮中，替换语言按钮，变为一个⚙️。
- [x] 一键部署脚本：`deploy.sh`，git pull 后自动 diff 变更文件判断是否需要重启服务（热更新模式下大部分改动无需重启；仅 go.mod/go.sum、package*.json、.env、compose/Dockerfile 变更才触发重启/重建）
- [x] 调整大厅的布局，进入大厅我们只需要显示已经存在的房间列表，最上方是“大厅”（保持不变）然后在大厅title下方的左边有一个按钮是刷新，右边是新建房间

- [x] 同理，好友也是一个单独的窗口，需要在左上角加入好友按钮进入。

- [x] 点击新建房间之后弹出一个新的窗口，分别让用户填写房间名：不填随机生成，盲注: 小盲 / 大盲 （默认是 1 / 2)， 然后是 买入： (默认是 200), 买入次数： （默认 3），然后是房间人数：最多8人最少2人，局数：不填就是无限，默认 20

- [x] 我们需要把房间名打印在房间的左下角（也就是用户屏幕的左下角，在聊天框上方一点点）

* bug: 当轮到对手操作时，他退出，然后系统帮他弃牌，然后我们应该在单挑局中直接获胜，但是竟然还需要我们操作一轮

- [x] 玩家在 all in 之后退出：不再当做弃牌处理，而是秀牌并留在牌局中比牌；若该退出方手牌更大则底池筹码直接消失（作为退出惩罚），否则留在桌上的一方正常赢得底池

- [x] 补齐退出场景的回归测试：2 人局 1 人退出（含面对 all in 对手）；3 人局 1 人退出（all in / 轮到他 / 没轮到他）；3 人局 2 人退出（一人 all in / 两人 all in）

- [x] all-in 摊牌时翻牌三张一起翻面，转牌、河牌各一张逐张翻

- [x] 开局后座位旋转：每位玩家自己显示在最下方，其他人相对位置不变；选座阶段仍按绝对位置排列

- [x] 座位整体略微内收（rx/ry 调小），为底部手牌与操作框之间留出更多空间

- [x] feature：翻牌并不需要一张一张翻牌，应该是发三张（然后一起翻面）

- [x] 新加入：修改用户名操作（大厅/个人战绩弹窗中修改，改名时同步迁移账号、历史战绩与头像）

- [x] 不再使用点击头像准备，改为牌桌上每人独有的准备按钮（toggle 切换准备状态）

- [x] 账号改为 UUID 唯一标识：注册需填写 UUID（至少 5 位数字/字母），用户名仅作昵称可重复；登录时用户名唯一则可用用户名登录，否则用 UUID；查看信息/添加好友均按 UUID 操作

- [x] bug：结算画面所有人的净输赢加起来不是 0 —— 中途离场的玩家不在结算名单里；现把离场玩家（离场时已结算过）也展示出来，总和归零

- [x] bug：make hot 热更新模式下 WebSocket 连不上、上传头像返回 404 HTML —— 前端 dev 服务器不代理 /api/* 与 /ws，现通过 NEXT_PUBLIC_API_BASE 让头像接口直连后端 3000，并清理了占用 3000 端口的残留 next dev 进程

* 我们需要添加提示音，每一次玩家操作播放音效。有没有那种专门为游戏开发的音效，按键音效

- [x] 提示音：接入 CC0 游戏音效（Kenney Interface Sounds），过牌/跟注/加注/弃牌/全下/发牌/赢池均有音效；⚙️ 设置面板新增音量滑块与静音开关（本地记忆）

* 我们需要把音量控制重新加入到设置中

- [x] 音量控制已重新加入设置面板（音效音量滑块 + 静音按钮）

* 当亮牌之后（或者在最后阶段开牌）把玩家的最大牌型显示在玩家框靠近牌桌一侧（请注意不要与手牌重叠）。

- [x] 摊牌时在玩家框靠牌桌一侧显示最大牌型（中英文本地化，如「葫芦」「顺子」），与筹码位置同侧、不与手牌重叠；摊牌节奏为先显示牌型，1 秒后弹出赢家 toast（显示 4 秒）

- [x] toast 展示期间保持上一把的亮牌与牌型显示（按 position 追踪亮牌，避免状态刷新后丢失），toast 消失/超时后自动进入下一局

- [x] bug：两人 all in 发完五张牌后直接弹 toast、没有摊牌展示 —— river 发完即结算并重置；现 river 发完只完成牌面，摊牌展示（亮牌+牌型+toast）完整播放后由下一次 deal 结算并开下一局

- [x] bug：空局状态下收到 player-call 导致服务端 panic 全进程崩溃 —— handleCall/Raise/Check/Fold 增加空玩家/越界保护

- [x] 离线踢人加固 + UT：`timeoutPlayer` 改为复用与「离开」按钮相同的 `evictPlayer` 路径（牌局中轮到时弃牌、all-in 留局比牌、牌局结束时释放座位；局间立即移除并让其余人回到未准备）。原实现在牌局中调用 `ResetToReadyPhase`，会把所有人的下注清零但不退回筹码，底池凭空消失。新增 `backend/server/table_test.go`（无需 Redis）：局间超时踢出、牌局中非轮到者超时、单挑轮到者超时、all-in 者超时、宽限期内重连取消踢出、被踢后 session 重放返回 `session-expired`。

- [x] bug 修复：房间最后一位真人离开后立即回收，离线玩家的座位保留 60 秒重连（`offlineTimeout`），超时请出并退款后若无人则立即回收。根因是页面重载时按本地 session 重新 `join-table`，服务端对不存在的房间会「按需新建」一个空房间，且旧座位 uuid 匹配不到任何人时不回任何消息，前端因此停在一个没有牌局状态、无法坐下的房间里。现在 session 重放的 join 带 `reconnect` 标记：房间不存在 / 座位已被释放 / 房间有密码但 session 未带密码 → 服务端回 `session-expired`，前端清除本地 session、弹出提示并回到大厅；座位仍在则原样恢复。

* 为了使得游戏更具有游戏性，我们需要增加加注或者平跟的动画。

- [x] 加注/跟注动画：下注/跟注时 3 枚筹码从该玩家座位飞向底池中心，座位上方同步弹出「+金额」标签。快照 diff 移至 socket 消息层（`web/lib/fxBus.ts` + `web/lib/tableFx.ts`），每条 `update-game` 逐帧触发，避免对手秒跟时中间帧被 React 批处理吞掉导致动画丢失。

* 然后加入收池动画。要考虑多人收池。

- [x] 收池动画：进入结算（Showdown）时从底池中心向**每位**赢家座位飞 4 枚筹码，遍历每个 pot 的 `winningPlayerNums`，天然支持多人收池与多边池。

- [x] 弃牌动画增强：玩家弃牌时在其座位上方弹出红色「弃牌/fold」标签，提示动作归属；下一手开始时清除所有瞬时标签。

- [x] bug 修复：跟注不出动画 —— 只有下注/加注和 all-in 有动画，跟注没有（双人局尤其明显，每次跟注都没有）。根因是服务端在一条 `update-game` 内就把「收口的跟注」直接发到下一街（`updateRoundInfo` → `deal`），stage 已 +1、各人 `bet` 已清零；而 `diffTableActions` 要求前后快照 stage 相同且 `betting` 仍为 true 才发 bet 事件，于是收口的跟注、河牌收口进 Showdown 的跟注、以及让所有人都无法再行动的 all-in 全部漏掉。改为以**前一帧**处于下注街（stage 2..5 且 `betting`）为条件，用整手累计的 `totalBet` 差值判定筹码投入，与 stage/`betting` 是否变化无关；盲注仍不算（前一帧是 NotReady）。河牌收口跟注与收池发生在同一帧时，收池动画延后 700ms，避免两股筹码对穿。座位上的筹码额度同样保留 900ms 再消失（`Seat.tsx` 的 `useDisplayedBet`），使跟注方的额度也会弹出。

- [x] bug 修复：动画座位错位 —— 自己坐 1 号位、对手坐 6 号位时，对手下注/收池动画却从 2 号位出现。根因是 `TableFx` 直接把玩家的游戏 position（行动位次）当作视觉槽位算坐标，而 `Table.tsx` 是按 seatID 排座、再以本人 seatID 旋转到槽位 0；position 与 seatID-1 并不相等。修复为新增 `seatSlot`：先把 position 解析成该玩家的 seatID，再按与 `Table.tsx` 相同的公式 `(seatID-1 - 本人seatID) % max` 计算视觉槽位；加注/弃牌/收池共用同一映射。已在「me 坐 1 号位、对手坐 6 号位」的真实对局中验证：对手下注筹码从其 6 号位（右上角槽位）飞出。

- [x] bug 修复：短 all-in（大于跟注、小于最小加注）不再直接结束该街。原现象：A 因筹码不足 all-in 时，B 被自动当作「已跟注」，牌局直接进入下一街，B 没机会跟注就被排除在底池之外，最后 A 直接赢走底池。根因是 `isCalled` 只看 `Called` 标记，B 在本街之前已行动过所以被视为已跟平；按座位顺序不同，要么 B 的 `totalBet` 低于 A 而被排除出底池（A 未经比牌直接赢），要么 A 的 all-in 被静默降级成跟注。现 `isCalled` 要求「已行动 **且** 下注额追平最高注」，落后于短 all-in 的玩家必须补跟或弃牌。新增 `TestShortAllInRaiseOpponentMustAct`（两种座位顺序）、`TestShortAllInRaiseOpponentMayFold`。
- [x] 下注规则按标准德州规则收口：短 all-in 合法但**不重开加注权**——自上一次完整加注后已经行动过的玩家只能跟注或弃牌（服务端拒绝其加注/all-in），尚未行动的玩家仍可加注，最小加注额以当前最高注（含短 all-in）为基准加上一次完整加注的增量。前端操作框在此情形下只显示「跟注/弃牌」。UT：`TestShortAllInThreeHandedRaiseRights`、`TestShortAllInDoesNotBlockUnactedPlayer`。
- [x] 最小下注/最小加注增量改为一个大盲（原为一个小盲）：翻前最少加注到 2×BB，翻后最少下注 1×BB。UT：`TestMinimumBetIsBigBlind`。
- [x] 边池与退码修正：A all-in 200、B all-in 100 时，A 多出的 100 无人能跟，在下注结束的当刻立即退回 A（不依赖座位下标，原实现按下标 0 初始化导致有时不退、有时退了但底池仍计入而凭空多出筹码）；退码后重建底池，并去掉金额为 0 的「幻影」边池（原来相同金额的 all-in 会多生成一个空池并给赢家多记一次胜场）。新增 `backend/poker/sidepot_test.go`，全部通过真实操作（发牌/下注/弃牌）驱动再定牌面摊牌：单挑不等额 all-in（深筹码为加注方 / 为跟注方）、三人 50/100/200 all-in 的主池+边池+退码（四种赢家排列）、弃牌者的死钱留在池中但不参与分配、活人跟两个 all-in、等额 all-in 只有一个池且只记一次胜场、平分主池+独立边池、跨街累积的边池；每个用例都断言桌面总筹码不变。
- [x] 只剩一名玩家可行动（其他人全部 all-in 且下注已追平）时，跳过该玩家的回合：下注直接结束，按 all-in 摊牌流程逐街自动发完公牌（原来该玩家要独自一街街过牌）。当他仍欠短 all-in 的筹码时回合保留（需要跟注或弃牌）。UT：`TestLonePlayerWithChipsGetsNoTurn`、`TestLonePlayerStillActsWhenOwingChips`。
- [x] 平分底池除不尽时的零头：按标准规则从庄家下一位开始顺时针每人一枚分给赢家（原实现零头直接消失）；前端赢家 toast 按同样规则显示金额。UT：`TestOddChipGoesToFirstAfterButton`（单挑 201 平分）、`TestTwoOddChipsHandedOutClockwise`（三人 302 平分）。

- [x] bug 修复：make hot 部署后登录、刷新页面，大厅显示的是账号 UUID 而不是用户名。根因是 `index.tsx` 回放登录时把 localStorage 里的 `gopoker-user`（账号 UUID）直接当作用户名派发，而服务端 `user-info` 回复里的 `username` 前端从未使用。现在用户名单独缓存到 `gopoker-username`（注册/登录/改名成功时写入），刷新后先用缓存名进大厅，服务端 `user-info` 回复为权威并回写缓存与房间 session；账号在服务端不存在（如 Redis 重置）时 `reconnect-user` 回 `session-expired`（tablename 为空表示账号级），前端清除本地登录并回到注册页。

- [x] 支持 HTTPS：`.env` 配置即可，两种模式——`TLS_DOMAINS`（Let's Encrypt 自动申请与续期，证书缓存在 `certs` 卷）或 `TLS_CERT_FILE`+`TLS_KEY_FILE`（自备/自签证书）。开启后应用监听 `HTTPS_PORT`（443，TLS 1.2+），原 8080 端口只负责 ACME 验证与 301 跳转到 https，并下发 HSTS；前端在 https 页面下自动使用 `wss://<同域>/ws`。不配置则保持纯 HTTP（适合前置反向代理/CDN 终止 TLS）。compose 新增 `${HTTP_PORT:-8080}:8080`、`${HTTPS_PORT:-443}:443` 端口映射与 `certs` 卷，Dockerfile 预建 `/app/certs`。UT：`backend/server/tls_test.go`（环境解析、证书文件模式真实 TLS 握手、ACME 模式的挑战路径不跳转 + 其余请求 301 去端口、IPv6 Host、HSTS 仅 TLS 下发）。

- [x] 保护服务器不受攻击（SYN flood / 拒绝服务等）。应用层（`backend/server/guard.go`，默认开启、`.env` 可调）：http.Server 超时（ReadHeader 10s / Read 30s / Write 60s / Idle 120s、16KiB 头上限）防 slowloris；每 IP HTTP 令牌桶限速（`RATE_HTTP_RPS`，超限 429）；WebSocket 每 IP / 全局连接数上限（`MAX_CONNS_PER_IP` / `MAX_CONNS`，握手前即拒绝）；每连接消息速率（`RATE_WS_MSGS`，超限以 1008 关闭）；登录/注册每 IP 每分钟尝试次数（`RATE_AUTH_PER_MIN`，防爆破）；房间数量上限（`MAX_TABLES`，防刷房耗尽内存）；WebSocket Origin 白名单（`ALLOWED_ORIGINS`）；代理头仅在 `TRUST_PROXY=true` 时采信，防伪造 IP；空闲 IP 记录 10 分钟后回收。内核/网络层（SYN flood 到不了用户态）：`deploy/sysctl-hardening.conf`（SYN cookies、backlog、重试次数、orphan/conntrack 上限等）+ `deploy/HARDENING.md`（nftables/ufw 限速示例、CDN 建议、公网上线清单）。UT：`backend/server/guard_test.go`（令牌桶补充/上限、429 按 IP 隔离与恢复、代理头信任、连接数上限与释放、登录限流、Origin 匹配、环境变量解析、空闲回收、房间上限）。

## bug

- [x] 当房间的牌局进行中，并且所有玩家都退出了房间，那么房间就进入了一种“dangle”状态，新人加入之后可以选择“加入下一局”，在长时间等待之后，房间就会被解散。

- [x]  我现在在用vivo V2066BA 测试，我发现我根本看不到底牌是什么，被左边为玩家挡住了，所以我们需要做一个按住牌桌查看底牌的功能。，然后在手机上看，扑克牌的花色都有点超出了牌的范围。

- [x] 底牌被遮挡 / 花色溢出：① 自己的座位提升 z 轴（`z-10`）置于相邻座位之上，窄屏下左侧座位不再盖住自己的手牌；② 新增「按住牌桌查看底牌」——在牌桌任意位置按住（手指或鼠标）约 0.2 秒后，自己的两张底牌放大显示在牌桌中央，松手即消失，普通点按座位 / 按钮不受影响，并屏蔽长按弹出的系统菜单；③ 手机端牌面字号收紧（点数 text-base、花色 text-xl、行高 tight）并加 `overflow-hidden`，原来点数 + 花色的行高之和超过 56px 卡高导致花色溢出牌底，桌面端尺寸不变。

- [x] 开局后其他玩家座位整体缩小（手机 82%、桌面 90%，以座位中心缩放，筹码动画落点不变），自己的座位保持原尺寸。

- [x] 牌桌可点击 + 按住弹出「牌面窗口」：窗口显示五张公牌（未发的街以占位牌表示），已入座时下方再显示自己的两张底牌。按住牌桌任意处约 0.2 秒即显示、松手消失；点击牌桌绿呢（felt）则固定显示，再点一次牌桌或窗口关闭；牌局未开始时不响应。

- [x] 牌面窗口 / 对手手牌窗口统一显示在屏幕上方（而非牌桌中央），避免被按住屏幕的手指遮住。

- [x] 手机端顶部重叠修正：「局数 1/20」胶囊与左上角「离开 / 投票结算」重叠 —— 手机上左上角按钮行下移到胶囊之下（桌面端不变）；胶囊在手机上不再逐人画投票圆点，改为「✓ 已投/在座」紧凑计数并缩小字号，避免顶到两侧工具栏；牌面 / 手牌弹窗下移到顶部工具栏之下（手机 top-28、桌面 top-24），不再与之重叠。

- [x] bug 修复：按住对手座位看牌后松手会弹出其个人资料 —— 松手时浏览器仍会派发一次 click 到座位。现在牌桌在捕获阶段拦截「紧随长按之后」的这次 click（`onClickCapture` + 标记），不再触发座位 / 准备 / 牌桌的点击行为；标记 300ms 后自动清除，长按后若没有 click 也不会吞掉下一次正常点击。

- [x] 长按不再出现文字选择与复制/粘贴气泡：全局关闭 `user-select` / `-webkit-touch-callout` 并去掉移动端点击高亮（这是游戏界面，长按是看牌手势）；输入框、聊天与日志面板（`.selectable`）仍可选中文字；房间内屏蔽长按 / 右键的上下文菜单。

- [x] bug 修复：手机上方块数字是蓝色但花色图案是红色、梅花是黑色而 PC 上是绿色 —— 手机字体把 ♦ ♣ 按 emoji 呈现，自带颜色、忽略 CSS。花色图案改为内联 SVG（`fill=currentColor`），四色牌在所有设备上一致（黑桃黑、红桃红、方块蓝、梅花绿）。

- [x] 公牌占位改为白色半透明卡框（`bg-white/15` + 白色细边），不再是深色半透明块。

- [x] 底池数字改为半透明胶囊（`bg-black/25` + 细白边、琥珀色数字带投影），绿呢纹理透出，不再像桌面上开了个洞。

- [x] 摊牌赢家提示（Winner +xxx）改为显示在屏幕底部（聊天标签之上，此时操作框已隐藏），不再盖住牌桌中央的公牌与亮出的手牌。

- [x] 按住对手座位可放大查看其手牌：显示该玩家名字与两张牌的放大版（未摊牌 / 未秀牌时为牌背——服务端本就不下发未公开的牌），摊牌或秀牌后显示真实牌面并标出牌型；松手消失。

- [x] 「投票结算」按钮文案改为「投降」（英文 Surrender），点击仍发起提前结算投票；手机上该按钮改为显示在「离开」按钮正下方、左缘对齐（桌面端仍为同一行）。「离开 / 投降」整体锚定在屏幕最左上角（手机端竖排后列很窄，与居中悬挂的「局数」胶囊互不重叠）。

- [x] bug：在 iPhone 上点击输入账号框页面自动放大、可上下左右拖动像网页 —— iOS Safari 会在聚焦字号 <16px 的输入框时自动把视口放大到该框，放大后整页可平移。现把注册 / 登录 / 邀请链接等所有小于 16px 的文本输入字号提到 16px（`Register.module.css .field input` 15→16、`game.css` 邀请链接 textarea 12.5→16；房间聊天 / 改名 / 新建房间输入本已是 16px），字号达标后 Safari 不再自动放大，保持原布局，像游戏而非网页。

- [x] bug：结算界面的头像显示不对 —— 结算画面（`SettlementModal`）只按 emoji 文本渲染头像，上传了自定义图片头像的玩家会被画成默认 🙂。现 `Settlement`/`SettlementModal` 透传并渲染账号头像：冠军卡、玩家行、最大底池三处的 `avatarFace` 在 `avatarImage` 且带 `uuid` 时用通用 `Avatar` 组件显示真实图片（带版本号防缓存），否则回退 emoji；无图片时样式与之前完全一致。已在真实对局中端到端验证（tester01 上传图片后结算行显示图片头像）。

- [x] bug：对话框的拉伸模式有问题 —— 聊天已与「战绩」统一为从按钮长成真正对话界面，不再是在按钮上方单独浮起的小窗（见顶部补丁）。

## to list

* 语音功能，我们应该把设置按钮也搬到房间界面，并且加入语音输入音量和其他人的麦克风音量的滚动条，然后在房间加入两个按钮，分别是麦克风和喇叭（已经上传在了public中）来控制是否打开麦克风（默认关闭），喇叭来控制是否听取其他人的语音（默认关闭），然后我们在每个人的profile中加入一个麦克风按钮，来单独禁止它的麦克风（这个麦克风只是对自己而言，房间里的其他人依然能够听见）。需要考虑PC端和手机端的界面的排列问题。

- [x] 语音功能：房间内点对点 WebRTC 语音（浏览器之间直连，服务端只转发信令）。服务端新增 `voice-signal` 消息（`backend/server/voice.go`）：按账号 UUID 在同一房间内单播 / 广播 join、leave、state、offer、answer、ice，服务端盖上 `from`（不可伪造），不在房间 / 未登录 / 未知类型 / 自己发给自己 / 广播 SDP 一律拒绝；客户端断开、离开、被顶号时服务端自动向房间广播 leave，让其他人立即拆掉连接。单帧上限从 1 KiB 提到 16 KiB 以容纳 SDP。UT：`backend/server/voice_test.go`（单播只到目标、广播不含发送者与匿名观众、伪造 from 被覆盖、各类拒绝、目标不在房间不外泄、unregister 广播 leave 且不发给同账号新连接、对方队列满时不阻塞）。前端 `web/lib/voice.ts`：麦克风或喇叭任一打开即加入语音网格并广播 join，其余在线成员发 offer；采用「perfect negotiation」处理两人同时加入的冲突；ICE 候选 150ms 合批以避开每连接消息限速；麦克风经 GainNode（输入音量滑块）后再发送，对方音频走 `<audio>` 元素（他人音量滑块 + 按人本地屏蔽）。房间右上角（PC 与手机两套布局）新增麦克风 / 喇叭两个按钮（默认均关闭，图标取自 public/microphone.svg、speaker.svg 内联为可着色组件，关闭时画斜线）；⚙️ 设置面板（大厅与房间共用）新增「麦克风输入音量」「他人语音音量」滑块并本地记忆；点击玩家头像的 profile 弹窗新增麦克风按钮，仅对自己屏蔽该玩家的语音（屏蔽名单本地记忆，房间其他人不受影响）；座位框右上角显示对方开麦 / 被我屏蔽的小徽标。

- [x] 自建 STUN/TURN：不依赖 Google 等第三方。三个 compose 文件（默认 / hot / dev）均新增 **可选** 的 `coturn` 服务（profile `voice`，在 `.env` 里设 `COMPOSE_PROFILES=voice` + `TURN_SECRET` 才会启动；缺 secret 时容器拒绝启动而不是变成开放中继；host 网络，`--use-auth-secret`，限额 + 拒绝所有内网段作为中继目标，防止被当作跳板）。不启用 coturn 时服务端返回空列表，浏览器只交换本机候选，同一局域网内语音照常可用，跨 NAT 则连不上；后端 `backend/server/turn.go` 新增 `get-ice-servers` → `ice-servers`：仅对已登录连接返回 coturn 的 STUN/TURN URL（`TURN_HOST` 未设置时用页面所在主机）及按账号签发、24 小时过期的 TURN 凭据（`<过期时间>:<账号>` + HMAC-SHA1(`TURN_SECRET`)），只设 `TURN_HOST` 未设 `TURN_SECRET` 时只给 STUN，两者都未设时返回空列表。前端进房即请求、临近过期自动刷新，收到回复前不猜任何地址（避免等待一个不存在的 STUN 超时）；`NEXT_PUBLIC_ICE_SERVERS` 仍可整体覆盖。`.env.example` 新增 `TURN_*`，`deploy/HARDENING.md` 新增需放行的端口（3478 UDP/TCP、49160–49200 UDP）。UT：`backend/server/turn_test.go`（凭据与独立 HMAC 计算一致、过期时间 = now+TTL、无 secret 只给 STUN、主机回退/优先级/IPv6、不同账号与不同时间凭据不同、环境变量解析与越界回退、页面主机校验、匿名请求被拒且恶意主机名不被反射进 URL）。

- [x] bug 修复：手机用 http://192.168.x.x:8080 打开时看不到麦克风按钮 —— 浏览器只在安全上下文（https 或 localhost）暴露麦克风 API，原实现把它当成「不支持 WebRTC」把两个按钮都隐藏了。现在只要有 WebRTC 就显示按钮：喇叭（收听）在 http 下照常可用；麦克风按钮变暗，点击弹出提示「麦克风需要 https 页面」。要在局域网手机上开麦，需按 `.env.example` Option B 用自签证书开启 https 并让手机信任该证书。

- [x] UI 修正：房间右上角麦克风 / 喇叭与 ⚙️ 并排成一行、尺寸一致（同一 `btn btn-icon` 规格）；设置 / 房间战绩弹窗改为 Portal 渲染到 body，不再被牌桌上的「点击准备按钮开始」提示盖住（原因：弹窗虽是 z-50，但父级工具栏 `z-10` 形成了层叠上下文）；座位下的「准备」按钮收窄（w-24 / sm:w-36，居中），不再撑满座位宽度。

- [x] 回声消除开关：语音使用浏览器自带的 WebRTC 音频处理（回声消除 / 噪声抑制 / 自动增益，Chrome 为 AEC3），并非自研算法。⚙️ 设置面板语音区新增「回声消除」开/关（默认开，本地记忆）；开麦状态下切换会用新约束重新打开麦克风并把新轨道换入每个连接（方向不变，无需重新协商）。

- [x] profile 弹窗（他人）标题栏新增「+」添加好友按钮，复用现有 `add-friend` 消息；已是好友时显示 ✓ 并禁用；服务端回 `user-info` 后好友列表即时刷新。

* 最右边增加一个 '...' ，点击会出现， '补码''观战','战绩'三个按钮，当我们再次点击 '...'就会消失，这三个按钮应该在操作按钮的layer之上，所以当玩家要操作时，就点击 '...'把这些按钮隐藏起来。

- [x] 房间右下角新增「…」按钮（`RoomMenu`）：点击展开「补码」「观战」「战绩」三个按钮（补码的 +/- 面板仍在其上方弹出），再点（此时显示 ✕）即收起；默认收起。菜单层级 `z-40` 高于操作框，需要操作时点「…」收起即可。原先散落的三个浮动按钮已移除。

* 我们要把聊天框默认隐藏，点击聊天按钮才出现，日志按钮同理

- [x] 聊天 / 日志默认收起：左下角只保留「聊天」「日志」两个标签按钮（房间名移到标签右侧同一行），点击标签展开对应面板，再点当前标签收起；聊天收起期间收到新消息时标签上显示未读数（进房时已有的消息不计）。

* 现在的计分板还是有些问题，首先对于一个输光了并且没有buyin次数的玩家，我们应该让她移步观众席，并且不能够上桌，提醒该玩家已经输光。然后还有一个计分bug，就是一位玩家输光但是还有buyin次数，当他buyin之后，不应该给他在计分板上显示两个名字。

- [x] 输光且无买入次数：局间自动移到观众席（原有），现在同时给该玩家 toast「你已输光且没有剩余买入次数，已转为观战」并在日志广播；房间新增按账号统计的整场买入台账（`backend/server/scoreboard.go` 的 `sessionLedger`），坐下 / 排队 / 补码都按账号本场累计买入校验 `MaxBuy`，输光的玩家再点空位或「加入下一局」会被拒绝并提示「买入次数已用完」（原来离座再坐下会重置买入次数，可绕过上限）；撤回买入会回退台账；结算 / 房间重置时清零。
- [x] 加注面板快捷额改为：最小 / ½ 底池 / 满池 / 2 倍底池（去掉 ¾ 底池）。

- [x] bug 修复：开局前添加又移除的机器人（或坐下就离开的玩家）仍出现在计分板 —— 离座会留下带买入的快照，但一手牌都没打过、筹码也没变。现在计分板 / 整局记录跳过「一手未打」的离场快照，与历史记录的规则一致。UT：`TestSettlementRowsSkipDepartedWithoutHands`。

- [x] 手机端房间名放在屏幕左下角（安全区内），不再悬在操作区上方。

- [x] bug 修复：在大厅（不在房间时）查看他人资料仍显示「屏蔽语音」麦克风按钮；现只在房间内显示。

- [x] 计分板只列出至少打过一手牌的账号：刚坐下 / 刚添加的机器人还没有结果，不出现在房间战绩、结算画面与整局记录里（服务端 `sessionPlayers` 与前端 `RoomStats` 同一规则）。

- [x] bug 修复：玩家「观战」再坐下，房间战绩里的筹码越来越多（来回几次连机器人的也涨）—— 观战 / 离座时那段的筹码已经退回钱包，但前端合并同一账号多段记录时把离场快照的筹码也加进「筹码」列，并用「筹码 − 买入」反推盈亏。现在「筹码」只算当前在座的那段，「盈亏」按每段 (筹码 − 买入) 求和，「买入」仍是全场累计；一手未打的离场快照不计（与服务端一致）。结算画面与历史整局记录按最终结果显示（筹码 = 买入 + 盈亏）。

- [x] 筹码图标重绘：原 `public/chip.svg` 是固定的亮绿色，在绿呢上几乎看不见。改为内联 SVG 筹码（带边缘缺口与内环），颜色随金额分档：< 50 银色、< 500 棕色、≥ 500 金色；座位筹码量、下注胶囊、右上角筹码数、加注面板金额、飞行筹码（按下注额 / 底池金额）全部按金额取色。

- [x] 下注动画的金色圆点改为筹码图案（`public/chip.svg`，飞行时带轻微旋转）；座位上的本街下注额移到座位正下方（每个座位位置一致），显示为「筹码图标 + 金额」的小胶囊；庄家按钮与摊牌牌型仍在座位朝向牌桌的一侧。

- [x] 操作按钮重新设计以贴合品牌视觉：新增 `.action-key` 系列（与 `btn` 同一表面梯度与圆角，触控目标更大）—— 过牌 / 跟注为翠绿实心、加注为品牌青色、全下为筹码琥珀色、弃牌为玫红描边、加注面板的「关闭」为中性灰；操作条与加注面板统一为 `tablehi` 面板 + 细边 + 琥珀色细环并淡入；加注面板的快捷额（最小 / ½ / ¾ / 满池）改为扁平 `btn-secondary`，滑杆改为青色。操作条只在轮到自己时出现，不再显示灰掉的按钮占位。

- [x] 「…」菜单（Bot / 补码 / 观战 / 战绩）点击菜单之外任意位置自动淡出关闭（160ms 渐隐），不必再点「…」收起。

- [x] bug 修复：计分板出现很多个「Bot Ace」—— 每次添加机器人都生成一个随机账号，移除再添加后，旧的离场快照与新座位是两个账号、无法合并，于是每一轮「移除 / 添加」都多一行。现在机器人账号由名字派生（「Bot Ace」永远是 `bot-ace`），同名机器人的多段记录合并成一行（买入、盈亏、统计求和）；锦标赛房间里买入次数已用完的名字会被跳过，下一个机器人用下一个名字。UT：`TestReaddedBotMergesIntoOneScoreboardRow`。

- [x] 牌局记录改为「整局计分板」：以前历史记录只在每位玩家离场 / 结算时写一条自己的数据，没有一个能打开的整局记录。现在每个房间会话有一份共享的 `SessionRecord`（Redis `gopoker:session:<id>`，保存 60 天）：所有参与者（含中途离场者，同一账号多次入座合并）的买入、盈亏与本局统计数据；玩家离场 / 输光 / 观战时立即重写一次（即使房间从未结算也有记录），结算时最后写一次并标记「已结算」。每条个人历史记录携带 `sessionId`。前端点击历史记录 → `get-session` → 弹出与结算画面相同的计分板（名次 / 皇冠 / 买入 / 筹码 / 盈亏），标题为房间名、副标题为时间与「已结算 / 进行中」；点击任一玩家弹出其**本局**的数据（手数、胜率、弃牌率、3bet、入池率、最大底池等）。只有该局参与者能读取记录，其他人得到「找不到该牌局记录」；旧的没有 `sessionId` 的历史条目仍按原样显示本人数据。UT：`TestSessionRecordPersistedOnLeaveAndSettle`（离场即写未结算记录且含全部参与者、再入座后统计与筹码合并、结算写「已结算」、结算后换新 id）；E2E `TestE2EHistoryOpensSharedSession`（两名玩家打到一人输光、投降结算 → 历史带 `sessionId` → 取回整局记录含每人统计且盈亏和为零、非参与者被拒）。

- [x] 机器人玩家：服务端托管的座位（`backend/server/bot.go`），本质是一个没有 socket 的 Client，走与浏览器完全相同的处理函数（跟注 / 过牌 / 加注 / 弃牌 / deal-game），引擎无感知。身份为账号 `bot-…`、头像 🤖、名字「Bot Ace / Bella / …」，买入不走钱包，`flushSession` 对机器人跳过（无历史、不写 Redis），投降投票只按真人计算多数，房间是否为空只看真人（最后一位真人离开后立即回收、机器人随房间销毁），没有真人在座时机器人不会自动准备（永远不会出现纯机器人牌局）。节奏：每次广播后安排一次「机器人 tick」（`scheduleBots` / `botTick`）——轮到机器人时思考 0.7～1.5 秒后行动；当第一个座位是机器人时由它像浏览器一样驱动 all-in 发牌（每街 0.9 秒）与摊牌后的下一局（5 秒）；局间自动补码（不超过买入上限）、重新入座（结算后）与准备，买入次数用完则自动离开。决策（`botDecide`）：翻前按手牌启发式分档（口袋对 / 高牌 / 同花连张…），翻后用新增的 `poker.HandStrength`（支持 3/4/5 张公牌的部分牌面评估）分档，按档位决定下注 / 跟注 / 过牌 / 弃牌，带随机性与偶尔的诈唬，所有动作都经过合法性约束（最小加注、短 all-in 后只能跟 / 弃、不超过筹码、能过牌时绝不弃牌）。
  - 只有房主（建房者；房主离开后由座位号最小的真人继承，`update-game` 新增 `host` 字段）可以管理机器人。「…」菜单里的「添加机器人」进入放置模式：空位显示「+」，点击即在该座位添加机器人；已入座的机器人显示红色 ✕ 并可点击移除；再点按钮（此时显示「完成」）退出；牌局开始或不再是房主时自动退出。服务端 `add-bot {seatID}` / `remove-bot {uuid}`，非房主返回「只有房主可以管理机器人」，座位被占返回「该座位已被占用」。
  - UT：`backend/poker/strength_test.go`（点数 / 花色解码、各街牌力评估与排序）；`backend/server/bot_test.go`（入座 / 命名 / 台账 / 不计入房间存活、开局中与满座拒绝、投票只算真人、无真人时闲置、400 步随机决策全部被引擎接受且能过牌时不弃牌、手牌分档、机器人在第一座位与只会跟注的真人连打两手且筹码守恒、房主专属与座位选择与房主继承）。

- [x] 严重 bug 修复：两名玩家几乎同时断线时服务端整个进程 panic（`send on closed channel`）。hub 在连接断开时关闭该客户端的发送队列，而牌桌可能还在向它转发消息（新加的语音「leave」广播让这个竞态变得容易触发，`broadcastToClients` / `clearClientUUID` 早已存在同样风险）。现在所有跨客户端发送统一走 `Client.trySend`（带锁、已关闭则忽略），关闭统一走 `closeSend`（幂等）。UT：`TestVoiceRelaySkipsClosedClients`。

- [x] 新增端到端测试 `backend/server/e2e_test.go`（需 `REDIS_URL` 指向一个可清空的 Redis，否则跳过）：两名真实 socket 客户端注册、建房、坐下、每手全下直到一人输光、充值、补码、投票投降必须被接受；以及补码后手机重连（新 socket 重放 `reconnect-user` + `join-table`）再投降。两种流程下「投降」均正常，尚未复现「你还没有入座」。

- [x] 点击牌桌固定显示的牌面窗口：现在点屏幕任意位置即可关闭（全屏透明遮罩），窗口右上角也有 ✕，不再依赖再次点到牌桌。

- [x] 默认盲注改为小盲 5 / 大盲 10（新建房间表单默认值、服务端未填时的默认值、隐式创建房间的默认配置一致）。

- [x] 新建房间增加「锦标赛」勾选框（默认关闭）：勾选后才有买入上限（「买入上限」输入框仅在勾选时显示，留空默认两次买入，不低于一次买入），玩家整场买入按账号累计、输光且无次数者转观战且不能再买入；不勾选则 `MaxBuy = 0`，补码 / 重进不限次数。服务端 `create-table` 新增 `tournament` 字段（`normalizeRoomConfig` 归一化），大厅房间列表带「锦标赛」标签（`tableInfo.tournament`）。UT：`TestNormalizeRoomConfigTournament`。

- [x] 计分板重名 bug：玩家离座（含输光被移到观众席）后再坐下，引擎里会有一条离场快照 + 一条新座位记录，结算画面与房间战绩各显示两行。现按账号合并为一行（买入求和、盈亏求和，`settlementRows`），所有人盈亏之和仍为 0；房间「战绩」面板同样按账号合并。UT：`backend/server/scoreboard_test.go`（同账号两段合并且总和为零、零买入离场记录跳过、无账号座位不互相合并、台账累计 / 撤回 / 越界钳制 / 重置 / MaxBuy=0 不限、输光者被移到观众席并收到提示且不能再买入）。

- [x] 结算画面按盈亏降序排列并显示名次（并列同名次），第一名金色皇冠、第二名银色皇冠、其余显示数字；每行同时显示该玩家本场总买入。

- [x] 结算画面与房间「战绩」合并为同一个计分板组件（`Scoreboard`）：名次 / 皇冠、头像、名字、买入、筹码、盈亏同一套列，按盈亏排名（并列同名次）；「战绩」按钮实时打开它，牌局结束时以「结算」为标题弹出并多显示一行最大底池。

- [x] 长按弹窗显示自己当前的最大牌型：新增前端牌型判定 `web/lib/handEval.ts`，按当前可见的牌计算——翻前只有 2 张手牌时按点数重复判定（高牌 / 一对），翻牌 5 张直接判定，转牌 / 河牌 6～7 张枚举所有 5 张组合取最高牌型（含轮子顺 A-2-3-4-5、皇家同花顺）；牌型名称与服务端一致并沿用中英文本地化。牌面弹窗在自己手牌下方显示，长按自己座位的弹窗也显示；对手仍只在摊牌 / 秀牌后显示服务端给出的牌型。

- [x] 「加入下一局」改为点空位预订：去掉牌桌中央的「加入下一局」按钮（原逻辑在局间把排队者自动入座并自动准备，导致新玩家还没反应过来下一局就开了）。现在观战者在牌局进行中直接点击空位，即预订该座位（`backend/server/reserve.go`，`take-seat` 在牌局进行中即为预订，按账号记录，断线重连不丢，离开房间即取消）；座位显示预订者头像 + 「下一局」标签，本人再点一次取消，他人点它提示「座位已被占用」，满桌判断计入预订。牌局结束（关闭摊牌的那次 deal-game）时把预订者买入坐到该座位（座位被占则顺延到第一个空位），**不自动准备**，因此 `autoStartIfReady` 不会触发，牌桌停在未准备状态等他点准备；钱包不足 / 买入次数用完的预订在入座时提示并作废。中央提示改为「点击空位加入下一局」/「已预订座位，下一局入座」。UT：`backend/server/reserve_test.go`（预订→局末未准备入座→准备后开局、切换 / 换座 / 各种拒绝、按账号跟随重连、付不起时作废并提示、update-game 携带预订）；e2e：`TestE2ESpectatorClaimsSeatMidHand`。

- [x] 补码弹窗：点「补码」后弹窗自动收起；若在牌局进行中（自己还在本局里）补码，弹出中性提示「补码成功，筹码将在下一局发牌时到账」（服务端本来就把局中补码记入 `PendingBuyIn`，下一局发牌时再加到筹码上，此前界面上筹码不变会让人以为没成功）。新增非错误样式的提示 toast（`appState.notice`）。

- [x] 安装设计稿的新座位与新加注面板（`tmp/new_seat`、`tmp/new_bet_layout`），按现有布局重新裁尺寸：
  - 座位（`web/components/Seat.tsx` + `web/styles/seat.css`）：头像圆盘骑在深色斜面信息板上沿，两张手牌扇在信息板上方，下注筹码胶囊在座位上方，D / SB / BB 作为头像角标（与 TURN / ALL-IN / FOLDED / WIN 状态叠加，不再用 🔔 标庄）。尺寸沿用原座位占位：手机宽 128px（牌 36×56）、桌面 224px（牌 64×96），名字 14/16px、筹码 15/18px；牌面仍用现有 `Card`（四色 SVG 花色、发牌 / 弃牌动画、遮牌），牌背换成设计稿的 `card_back.svg`，筹码图标沿用按面额上色的 `Chip`，头像沿用 `Avatar`（支持图片）。保留全部原有行为：点座位看战绩 / 秀牌 / 移除机器人、长按放大、准备遮罩与准备按钮、麦克风角标、离席灰显、摊牌牌型标签、赢家金色高亮。
  - 加注面板（`web/components/RaiseInput.tsx` + `web/styles/raisepanel.css`）：深蓝灰 + 细金边、顶部金线、四个快捷键（最小 / 1/2 底池 / 满池 / 2 倍底池，选中项暖金）、金色滑杆（原生 range，去掉了 Mantine Slider）、− / + 按大盲步进、当前底池与金额用面额色筹码，确认键就是操作栏同款 BET / ALL-IN 材质键，底部箭头指向操作栏。面板宽度与操作栏对齐（桌面 ≤480px，手机 100vw−22px），字号与操作键一致（主 13px / 副 7px，圆角 15px）。面板打开时操作栏留在下方（原来是整条替换），轮次一过自动收起。
  - 后续微调：信息板加高、文字在牌下方居中（原来筹码行压在底边上）；操作栏整条不再拦截点击（右下角菜单在自己回合也能点）；牌局进行中不再画「空位」（只有可预订的观战者才看到可点的空位，已预订的座位所有人都能看到）；下注时去掉座位上方弹出的「+金额」标签（座位自带下注胶囊已显示）；摊牌时的牌型标签移到该玩家手牌之上，半透明胶囊，与下注胶囊同款式；去掉底部「赢家 +xxx」toast，改为赢家座位上名字位置显示「+xxx」（摊牌展示期间），结束后恢复名字（底池被没收的情况仍保留提示）。
  - 未做：`tmp/` 目录仍保留在仓库里，删不删由你决定（已在 b7643c2 提交，随时可找回）。

- [x] 操作时限：新建房间时可设「操作时限（秒）」（默认 40，0 = 不限，服务端限制在 5～300 秒）。服务端 `backend/server/clock.go`：每次广播为当前「轮次」（局数 / 街 / 行动位 / 座位 uuid）对表——同一轮次保持原截止时间（聊天、预订等引起的重播不会重置计时），新轮次重新计时，无人行动时清除；到点时若可过牌则替玩家过牌，否则弃牌，并广播日志「X ran out of time and checks/folds」。`update-game` 携带 `actionTimeout` 与 `actionRemainingMs`（剩余毫秒而非绝对时间，手机时钟不准也能正确倒计时）。前端在轮到的玩家座位底边显示设计稿里的倒计时进度条（金色，最后 1/4 变红）。UT：`backend/server/clock_test.go`（面对下注超时弃牌 / 可过牌超时过牌、同轮重播不重置 / 新轮刷新 / 无人行动清除、消息携带、关闭时无影响与取值范围）。

## 操作栏（Operator）设计说明

当前实现（`web/components/Input.tsx`、`RaiseInput.tsx`、`InputButton.tsx`，样式 `styles/index.css` 的 `.action-key*`）：

- **只在轮到自己时出现。** 不是自己的回合时操作栏完全不渲染（之前是一排灰掉的占位按钮）。轮到谁由座位框的高亮脉冲提示；操作栏出现本身就是「该你了」的信号，配合淡入动画，注意力自然落到底部。
- **一个动作一种颜色，同一套暗色表面。** 深炭灰底 + 暗金描边，按风险着色：过牌 / 跟注 = 灰蓝（安全、次要），加注 = 金棕（主动作），全下 = 橙金（高风险强提醒），弃牌 = 暗红（危险，单独一行），加注面板里的「关闭」= 中性炭灰。每键带一个小线性图标辅助识别。键高 48/56px、圆角 14px，手机上单手可达。
- **按钮组合随规则变化。** 面对短 all-in（不重开加注权）时只显示「跟注 / 弃牌」；能过牌时第一个键显示「过牌」，否则显示「跟注 + 金额」，金额就在键上，不用再看底池。
- **加注面板是两段式。** 先点「加注」进入面板：左侧是金额（筹码图标按金额取色）+ 快捷额「最小 / ½ 底池 / 满池 / 2 倍底池」+ 滑杆，右侧是确认键与关闭键。确认键在金额达到全下时自动变成琥珀色的「全下」，所以不会出现「以为在加注其实是全下」。快捷额都会被夹到 [最小加注, 全下] 区间内。
- **与「…」菜单的层级关系。** 「…」菜单（Bot / 补码 / 观战 / 战绩）叠在操作栏之上，但点击菜单之外任意位置自动淡出，所以轮到自己时不需要先手动收起。

想法 / 后续可做：

- **预选动作（pre-action）。** 不是自己回合时提供「过牌/弃牌」「跟任意注」「跟注 X」三个勾选项，轮到自己时按预选自动执行，能显著加快多人局节奏。需要前端在 `update-game` 到达且 `action == me` 时立即发送对应消息，并在下注额变化时自动取消「跟注 X」。
- **回合计时。** 服务端每回合计时（如 30 秒），操作栏顶部一条倒计时进度条，超时自动「过牌 / 弃牌」。这是目前唯一能防止一个人挂机拖垮全桌的手段（现在只有离线 60 秒踢人）。计时应由服务端权威驱动，前端只渲染剩余时间。
- **全下二次确认。** 全下不可撤销，手机上误触代价最高。可以做成按住 0.4 秒确认，或第一次点击变成「确认全下？」再点一次。
- **加注面板记忆。** 记住玩家上次用的快捷额（如总是用 ½ 底池），下次打开面板默认选中；或允许在设置里自定义四个快捷额的倍数。
- **键盘快捷键（PC）。** C 过牌/跟注、R 加注、A 全下、F 弃牌、数字直接输入金额、回车确认。
- **动作确认反馈。** 点击后按钮短暂进入「已发送」态（禁用 + 微缩），直到下一条 `update-game` 到达，避免网络抖动时重复点击发出两次消息（服务端虽会拒绝第二次，但会记一条 warn 日志）。
- **手机横屏。** 目前操作栏与聊天标签共享底部区域；横屏时高度紧张，可以让操作栏浮在牌桌右下、聊天标签留在左下。

## 状态

OK，这一次直接两个人all in 发完牌直接卡死。我觉得我们的状态是不是有些混乱，牌局会有如下几个状态：
0. 未准备状态：允许补码，允许换座，允许直接进入观战席。当所有玩家（≥2 人）都准备后进入发牌状态。
1. 发牌状态（PreFlop）：一次性给每位玩家发 2 张手牌，小盲/大盲自动下注，然后从 UTG 开始轮流操作，直到所有非弃牌玩家跟平（或全部 all-in），进入翻牌状态；若只剩一位玩家未弃牌，直接跳到结算状态。
2. 翻牌状态（Flop）：一次性发三张公牌（三张一起翻面），从庄家下一位开始轮流操作。跟平后进入转牌状态；只剩一人未弃牌则直接结算。
3. 转牌状态（Turn）：发一张公牌，轮流操作。跟平后进入河牌状态；只剩一人未弃牌则直接结算。
4. 河牌状态（River）：发一张公牌，轮流操作。跟平后进入结算状态；只剩一人未弃牌则直接结算。
5. 补充——all-in 的处理：所有未弃牌玩家都 all-in、或只剩一名玩家还有筹码且下注已追平（没有人能再与他对抗）后，后续每一街的「玩家轮流操作」全部自动跳过，流程照常走：跟平 → 发下一街公牌 →（无人可操作，自动过）→ 下一街……本质上就是自动走完剩下的流程。实现上由客户端按固定节奏（每街 900ms）请求发牌，直到牌面发满。然后结算。
   - 下注规则：最小下注 / 最小加注增量为一个大盲，之后每次完整加注以其增量作为新的最小加注。all-in 永远合法：不足跟注额的 all-in 视为「跟注不足」；超过跟注额但不到完整加注的「短 all-in」不重开加注权——已在本轮行动过的玩家只能跟注或弃牌，未行动过的玩家仍可加注（以当前最高注为基准）。
   - 退码：下注结束时若无人能再跟最高注（对手全部 all-in），最高注者多出的部分立即退回其筹码，底池按每位 all-in 玩家的累计投入分层为主池/边池，弃牌者的筹码作为死钱留在对应层中。
6. 结算状态（showdown）：展示所有参与比牌玩家的手牌并判别大小（当只有一名玩家没有弃牌是，无需展示除非他点击秀牌），每个底池/边池分别在其有资格的玩家中比牌；平分时除不尽的零头从庄家下一位起顺时针每人一枚分给赢家。显示每位参与比牌玩家的最大牌型（随翻牌动画同时出现），等待 1 秒后弹出 toast 显示胜利玩家及筹码数量，toast 持续 4 秒；toast 期间牌桌保持亮牌与牌型展示；toast 消失后在胜利玩家框靠牌桌一侧显示 +xxx，随后（结算总计约 5 秒）进入下一状态：
   1. 当有玩家筹码为 0、有玩家预订观战、有玩家退出房间、观战席有人排队上桌 → 进入未准备状态（0）
   2. 其他情况自动开始下一局（回到发牌状态 1）
7. 终结状态：当局数耗尽，或投票提前结束对局，进入终结状态。弹出房间计分板（所有人净输赢总和为 0，中途离场玩家也在列），玩家关闭计分板后可回大厅，房间在所有人离开后销毁。

玩家状态：
1. 未准备状态：允许补码，允许换座，允许直接进入观战席。
2. 准备状态：点击准备按钮从未准备进入（未开局时可再点取消）；已准备后不允许进入观战席（按钮失效）、不允许补码（按钮失效）。所有玩家都准备后自动开局。
3. 游玩状态：轮到该玩家时操作框亮起，可过牌/跟注/加注/全下/弃牌。
4. all-in 状态：不再参与下注操作，轮到时自动跳过回合；可选择秀牌（展示手牌），并保留在局中参与最终比牌。
5. 观战状态：牌局进行中点击空位即「预订」该座位（座位上显示预订者头像与「下一局」标签，再点一次取消，别人不能再选它）；本局结束时自动买入坐到该座位，但处于**未准备**状态——因此牌桌回到未准备状态（0），由新玩家自己点准备后才开局，不会在他还没准备时自动开下一局。要求筹码数大于 0 或可充值（受房间最大买入限制），且桌子未满（含已预订的座位）；牌局进行中不能直接坐下。
6. 离线状态：断开连接后 60 秒内重连恢复原状态。超过 60 秒：未 all-in 的玩家轮到他时自动弃牌，并在下一局开始前被请出房间；all-in 玩家保留在局中秀牌比牌，若其本应赢得底池，底池被没收（筹码消失，作为离开惩罚）。

补充（当前已实现的相关机制）：
- 提前结算投票：≥半数玩家投票后，当前牌局打完即进入终结状态（计分板）。
- 中途退出的玩家：退出当刻按当刻盈亏结算回账号并计入历史战绩；最终计分板也会列出他们（保证所有人净输赢总和为 0），不会重复结算。
- 修改用户名只能在未入座任何房间时进行（大厅/个人战绩弹窗）。
